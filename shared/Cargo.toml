[package]
name = "daglock-shared"
version = "0.1.0"
edition = "2021"
description = "Shared constants and validation utilities for DagLock"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "1.0"
hex = "0.4"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
pretty_assertions = "1.4"
